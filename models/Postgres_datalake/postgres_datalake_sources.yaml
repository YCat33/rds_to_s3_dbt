version: 2

sources:
  - name: demo_postgres_aws
    description: Burst Bank data in Postgres.
    database: demo_postgres_aws
    schema: burst_bank
    freshness: # default freshness
      warn_after: {count: 700, period: day}
      error_after: {count: 1000, period: day}
    loaded_at_field: loaded_at    
    

    tables:
      - name: customer
        description: Customer information for the Burst Bank Schema.